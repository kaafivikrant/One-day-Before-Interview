#include <iostream>
#include <utility>
#include <vector>
#include <numeric>
#include <queue>
#include <cmath>
#include <map>
#include <string>
#include <cstring>
#include <algorithm>
#include <fstream>
#include <unordered_map>
#include <unordered_set>
#include <set>
#include <list>
#include <stack>
#define ll long long
#define dd double
#include <stack>
#include <chrono>
#include <thread>
#include <iomanip>

using namespace std;


int main(){

    //Bitwise left shift operator x2
//    int x = 10<<1;
//    cout<<x;

    //Bitwise right shift operator /2
//    int x = 10>>1;
//    cout<<x;

    //Bitwise not operator -1 to n , ~10 -> -11
//    int x = ~10;
//    cout<<x;

    // Check even or odd

//    int num = 10;
//    if((num & 1) == 0){
//        cout<<"Even";
//    }else{
//        cout<<"Odd";
//    }

    // Swap two numbers

//    int c = 10;
//    int d = 20;
//    cout<<c<<" "<<d<<endl;
//
//    c = c^d;
//    d = c^d;
//    c = c^d;
//
//    cout<<c<<" "<<d<<endl;

    // Check the ith bit of the number

//    int num = 10;
//    int x = num & (1<<(3-1));
//    x?cout<<"Yes":cout<<"No";

    //Flip the ith bit of the number
//
//    int num = 4;
//    int x = num ^ (1<<(1-1));
//    x?cout<<"Yes":cout<<"No";
//    cout<<num<<" ";
//    cout<<x; //5

    // Trun on ith bit

//    x = num | (1 << (i-1));

    //Turn off ith bit

//    x = num & ~(1 << (i-1));

//    int um = 64;
//    int x = um & (um - 1);
//    (!x)?cout<<"Power of 2": cout<<"Not power of 2";


    //Converting the uppercase to lowercase
//    char a = 'A';
//    char A = a | ' ';
//    cout<< A;

    //Converting lowercase to uppercase

//    char a = 'a';
//    char A = a & '_';
//    cout<<A;

    // Min and Max
//
//    minimum = y ^ ((x ^ y) & -(x < y)); // min(x, y)
//
//    maximum = x ^ ((x ^ y) & -(x < y)); // max(x, y)
//

    

    return 0;
}